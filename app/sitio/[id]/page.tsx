import PlaceholderScreen from "@/components/PlaceholderScreen";

export const metadata = { title: "Estado del lugar — Suyu" };

export default function SitioPage() {
  return (
    <PlaceholderScreen
      title="Estado del lugar"
      note="El detalle del sitio con aforo, alternativa y servicios llega en B8."
    />
  );
}
