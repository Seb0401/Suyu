import PlaceholderScreen from "@/components/PlaceholderScreen";

export const metadata = { title: "Itinerario del día — Suyu" };

export default function ItinerarioPage() {
  return (
    <PlaceholderScreen
      title="Itinerario del día"
      note="El planificador con paradas y progreso de visita llega en B10."
    />
  );
}
