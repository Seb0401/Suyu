import PlaceholderScreen from "@/components/PlaceholderScreen";

export const metadata = { title: "Ruta accesible — Suyu" };

export default function RutaPage() {
  return (
    <PlaceholderScreen
      title="Ruta accesible"
      note="El buscador de rutas con mapa y línea de tiempo llega en B6."
    />
  );
}
